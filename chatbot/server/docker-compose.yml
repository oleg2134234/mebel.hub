services:
  chatbot-backend:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - chatbot-leads:/app/data

volumes:
  chatbot-leads:
