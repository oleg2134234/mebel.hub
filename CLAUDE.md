# mebel.hub — правила проекта

Одностраничный статический каталог мебели для менеджеров по продажам.
Живёт по адресу **https://oleg2134234.github.io/mebel.hub/** (GitHub Pages, ветка `main`, корень репозитория).

## Обязательные правила

Нарушать нельзя, проверять на каждое изменение:

1. **Главное правило — один и тот же узнаваемый диван на всех слайдах карточки.** Модель нельзя перерисовывать, улучшать или подменять даже ради красивого кадра, белого фона или правильных выносок. Подробная проверка идентичности — ниже.
2. **Порядок слайдов в галерее** — фиксированный, менять местами запрещено. Полный список ниже, в разделе «Порядок слайдов в галерее».
3. **Большие видео — вне репозитория.** В git не коммитим видеофайлы тяжелее ~5 МБ и вообще ничего из папок `reels/`, `social/`, `story/`, `video/`. Подробности ниже, в разделе «Медиа и большие файлы».
4. **Габариты товара — длина и ширина обязательны** в `dims`, иначе товар выпадает из подбора. Подробности ниже, в разделе «Данные каталога».

### Визуальная идентичность модели — правило № 1

На каждом слайде одной карточки должен быть **тот же самый диван**, однозначно узнаваемый как одна модель. Допустимы только другой ракурс и штатные состояния этой же модели: сложенный, разложенный, открытый короб, крупный план.

Не должны меняться: силуэт и конструкция; число и форма модулей; пропорции; ширина подлокотников; форма спинки, сиденья и подушек; количество подушек; швы, стёжка и кант; ножки; полки и декор; ткань, её фактура и цвет. Нельзя добавлять или удалять детали, менять конфигурацию, превращать прямой диван в угловой/П-образный либо использовать похожую модель.

**При конфликте требований идентичность дивана важнее всего:** важнее белого фона, композиции, качества изображения, расположения линий и других правил. Если исправление меняет диван или точное совпадение нельзя подтвердить сравнением с остальными слайдами, результат не подключаем. Сначала ищем реальный кадр этой модели или редактируем только фон/графику детерминированно, не трогая пиксели предмета.

Перед подключением нового изображения сравниваем его минимум с главным реальным фото и другими ракурсами карточки: отдельно проверяем перечисленные выше признаки. Генеративный результат не принимаем по общему впечатлению «похож» — совпадение должно подтверждаться по деталям.

**ПОДТВЕРЖДЕНО 14.09.2026 — цвет/материал для промпта берём с реального фото, а не из `desc`.** При генерации слайдов «Уют» для 20 кроватей выяснилось, что у части товаров партии Apogey текст `desc` называет не тот цвет обивки, что на самом деле на фото — не оттенок, а другой цвет: «Кордова НС 1» (текст «серый», фото бирюзовое), «Кордова НС 2» (текст «серый», фото кремовое), «Кордова НС 3» (текст «синий», фото сиреневое), «Моно 1» (текст «зелёный», фото бежевое), «Лайт» (текст «синий», фото серое); у «Невада 1» вообще два разных изголовья на разных слайдах одной карточки — похоже, там вставлено фото другого товара линейки. Прежде чем писать в промпт цвет/фактуру/рисунок изголовья, всегда открываем реальное фото этой карточки (`slide2_hero.jpg`/`slide1_interior.jpg`) и смотрим глазами — `desc` может быть устаревшим или скопированным не с той карточки. При расхождении текста и фото для генерации ориентируемся на фото (чтобы новый слайд совпадал с остальными слайдами той же карточки), а расхождение фиксируем отдельно в журнале, не смешивая с текущей задачей.

**После каждого готового исправления обязательно показываем справа в браузере полную карточку товара**, открыв её галерею на исправленном слайде. Недостаточно открыть прямую ссылку на файл изображения: пользователь должен видеть результат в реальном интерфейсе карточки, с названием товара, номером слайда и подписью. Карточку показываем до публикации, чтобы результат можно было проверить.

**При повторной замене уже показанного изображения обязательно меняем имя файла/версию URL** (`slide_dims-v2.png` → `slide_dims-v3.png`) и обновляем ссылку в галерее. Простое перезаписывание файла под прежним URL запрещено: браузер может оставить старую картинку в кэше. После изменения перезагружаем страницу и заново открываем полную карточку на исправленном слайде.

**Единственное явно одобренное пользователем исключение на 04.09.2026 — `43 Марк ДК`:** разрешено использовать `assets/mark-dk/slide5_dims-v2.png`. Это точечное решение не становится образцом и не ослабляет правило идентичности для других карточек.

## Что это технически

- **Один файл — `index.html`** (~1600 строк): весь CSS и весь JS встроены прямо в него. Больше кода нигде нет.
- **Нет сборки, нет зависимостей, нет `node_modules`, нет тестов, нет линтера.** Открыл файл — отредактировал — сохранил.
- Всё остальное в репозитории — медиа: фото и видео товаров.
- Внешние ресурсы не подключаются (шрифты системные, скриптов с CDN нет). Так и оставляем — сайт должен работать офлайн из папки.

## Структура

```
index.html              единственный код
assets/<модель>/         фото и видео карточек товара (slide*.jpg, video_*.mp4, scheme_*.jpg, real*.jpg)
assets/brand/            логотип и иконки интерфейса (logo.jpg, icons/*)
reels/  social/  story/  video/   материалы для соцсетей (в самом сайте НЕ используются — см. «Медиа и большие файлы»)
tools/serve.pl           локальный статический сервер для предпросмотра
sync.ps1 / sync.sh       синхронизация между компьютерами
```

## Данные каталога — внутри `index.html`

Три JS-объекта в `<script>` (примерно строки 727–729), каждый — JSON в одну строку:

| Объект | Что это | Ключ |
|---|---|---|
| `PRODUCTS` | массив товаров (сейчас 44 шт.) | `id` (число) |
| `IMAGES` | id → путь к главному фото карточки | `id` |
| `GALLERIES` | id → `{title, slides:[{src,name,desc,type?}]}` | `id` |

Поле товара: `{id, title, category, dims, desc, colorIdx, hero?}`.

- `category` — одно из: `sofa`, `armchair`, `bed`, `mattress`, `other` (человеческие названия в объекте `CATS`).
- `dims` — строка `"Длина×Ширина×Высота"` в миллиметрах, например `"2360×1080×900"`. Диапазоны пишутся через тире: `"2000×800–1800×230–240"`.
- `colorIdx` — индекс в демо-палитре подбора по цвету.
- `hero: true` — карточка крупнее в каталоге.

### Порядок создания новой карточки товара

Этот процесс обязателен для запросов «сделай карточку», в том числе когда пользователь передал только папку с фото или видео.

1. **Разобрать исходники.** Найти все фото, видео и документы в указанной папке, просмотреть их и определить точную модель и её варианты. Если в одном ролике показано несколько вариантов, не смешивать их в одной карточке; выбрать нужный вариант по запросу пользователя.
2. **Проверить факты.** Сверить название, габариты, спальное место, механизм, наполнение, опоры и хранение с материалами пользователя и надёжной страницей производителя/поставщика. Не добавлять неподтверждённые свойства. В `dims` обязательно записать длину и ширину в миллиметрах.
3. **Подготовить реальные материалы.** Отобрать главный общий вид и доступные ракурсы; отдельно подготовить разложенный вид, спальное место и хранение. Кадры из видео можно извлекать, обрезать и сжимать, но в описании честно указывать, что это кадр видео. Не выдавать фото другой версии модели за текущую.
4. **Сделать первые три слайда.** «В интерьере» по реальному референсу → «Уют» с человеком и питомцем → двухпанельные «Размеры» по правилам ниже. На визуализациях точно сохранять форму, цвет, ткань, пропорции, швы, подушки, опоры и другие признаки товара; в описании называть их визуализациями.
5. **Собрать галерею.** Добавить остальные подтверждённые материалы в фиксированном порядке из следующего раздела. Позицию без подходящего источника пропустить: не генерировать фиктивный разрез, задний ракурс, механизм или бельевой короб.
6. **Обновить каталог.** Добавить товар в `PRODUCTS`, главное фото в `IMAGES`, галерею в `GALLERIES`; использовать новый уникальный `id` и отдельную папку `assets/<модель>/`. Главное фото — «В интерьере» или реальный общий вид.
7. **Проверить результат.** Убедиться, что все изображения и видео загружаются, порядок и подписи верны, карточка открывается на широком и мобильном экране, а прежние товары и код не изменены случайно. Каждый медиафайл должен укладываться в ограничения раздела «Медиа и большие файлы».
8. **Публиковать только по явной просьбе.** До публикации оставить карточку локально. По команде «опубликовать/запушить» сохранить резервную копию, закоммитить только файлы карточки и связанные изменения `index.html`, отправить в `main`, дождаться GitHub Pages и проверить публичные URL главного фото, размеров и видео.

### Габариты — длина и ширина обязательны (правило 3 из обязательных)

**У каждого товара в `dims` должны быть и длина, и ширина.** Подбор («квиз») фильтрует по длине и ширине; товар без любого из этих чисел просто не появится в результатах. Длина = большее из двух напольных измерений, всегда ≥ ширины. Высота в подборе не участвует. У матрасов ширина берётся из диапазона (`parseRange(dims, 1)`).

Формат дивана (прямой / угловой / П-образный) определяется не отдельным полем, а по тексту `title`/`desc`: `ДКУ`, `оттоман`, `угловой` → угловой; `п-образ` или суффикс `ДКУ-П` → П-образный (см. `matchesFormat`).

### Порядок слайдов в галерее (правило 1 из обязательных)

Слайды в `GALLERIES[id].slides` идут строго в этом порядке. Пропускать позицию можно (не у всех моделей есть все слайды), **менять местами — нет**.

1. **В интерьере** — визуализация модели в интерьере. Всегда первый.
2. **Уют** (или «Уютный вечер») — жанровая сцена с моделью в быту.
3. **Размеры** — слайд с габаритами в сложенном виде.
4. **Разрез** — разрез / рентген конструкции. Если есть.
5. **Видео: раскладка «…»** — схематичное видео механизма (если раскладка есть). Идёт сразу после «Размеры»/«Разрез». Реальные видео («облёт», реальная съёмка) — тоже здесь.
6. **Реальные фото товара** — по кругу вокруг предмета: «Общий вид» → «Вид спереди» → «Ракурс 3/4» → «Вид сбоку» → «Вид сзади».
7. **Разложенное состояние** — «В разложенном виде» → «Раскладка» → «Спальное место».
8. **Хранение** — «Бельевой ящик» / «Бельевой короб» (если справа и слева — сначала справа).
9. **Преимущества и особенности** — индивидуальные достоинства модели, после хранения и перед «Практичностью». Обязателен для каждой карточки, проходящей доработку.
10. **Практичность** — закрывающий слайд. Всегда последний, если присутствует.

Коротко: сначала блок «визуализации» (интерьер → уют → размеры → разрез → видео механизма) в фиксированном порядке, затем реальные фото (обход вокруг модели), затем раскладка и спальное место, затем хранение, «Преимущества и особенности» и «Практичность» в самом конце.

Первый слайд карточки в каталоге (`IMAGES[id]`) — это, как правило, «Общий вид» или «В интерьере»; он не обязан совпадать с первым слайдом галереи, но должен быть реальным фото или интерьерной визуализацией, не «Размерами» и не «Разрезом».

### Как делать слайд «Размеры» (обязательный процесс)

**ПОДТВЕРЖДЕНО 10.09.2026 (Джой 7 ДКУ-О, id 226 — старый слайд отклонён):** слайд «Размеры» можно делать **одним слайдом (две панели) или двумя отдельными слайдами** галереи («Размеры — сложенный вид» / «Размеры — разложенный вид»). В любом варианте обязательно:
- Исходники — **кадры сложенного и разложенного вида этой же модели, приведённые к сплошному белому фону `#FFFFFF`**. Серый/студийный фон не годится — сначала убрать фон до чистого белого (Nano Banana: «замени фон на белый, диван не трогай»), только потом наносить выноски.
- **Утверждённая раскладка и стиль (Олег, образец от руки 10.09.2026 для Джой 7, затем «не трогай линии и их расположение, просто проставь размеры в стиле других слайдов с кружочками на конце»):**
  - **Длина / ширина** — одна прямая линия **ПОД диваном, во всю ширину**, почти горизонтальная (лёгкий наклон по перспективе пола).
  - **Глубина** — **короткая диагональ в пустом поле у дальнего торца**, параллельная уходящей боковой грани в перспективе. Сложенный → сверху-справа у кушетки. Разложенный → снизу-слева у торца с валиком. Глубину НЕ рисуем линией по полу под диваном и НЕ выносим в подпись.
  - **Стиль — как на других слайдах каталога: голая линия + кружок на каждом конце + тёмная овальная плашка с белым числом. Выносных линий к углам НЕ рисуем** (Олег их забраковал). Числа — только цифры, в см.
  - Когда Олег уже одобрил ПОЛОЖЕНИЕ линий — «просто проставь размеры» значит оставить их ровно там же и добавить только кружки и плашки, геометрию заново не пересчитывать.
  - Реализация: `tools/render-joy-7-dims.py` (id 226 → `slide_dims_folded.jpg` / `slide_dims_unfolded.jpg`, коммит `006f340`). `dim()` — нижняя линия во всю ширину, `dim_explicit()` — диагональ глубины, флаг `EXT=False` отключает выносные линии. Исходники без фона — `assets/joy-7-dku-o/dims_white_{folded,unfolded}.jpg`.
- Числа — из `PRODUCTS[226].dims` (сложенный: длина×глубина, высота только в подпись) и `desc` (спальное место: ширина×глубина), в сантиметрах.
- Раньше так же был отклонён серый слайд с косыми линиями у «Бали 7.2» — это повторное нарушение, лечится только детерминированным построением (PIL/SVG), см. ниже.

**ПОДТВЕРЖДЕНО 14.09.2026 (кровати Apogey, id 238–253) — правило действует для ЛЮБОЙ мебели, не только диванов:** положение размерных линий задаёт Олег (вручную или по утверждённому образцу); Claude только проставляет цифры — кружки на концах и тёмную плашку с числом — поверх уже одобренной геометрии, самостоятельно линии не строит и не подбирает координаты на глаз. Причина: при попытке автоматически расставить линии по фото 16 новых кроватей без готового образца одна из линий (Кордова НС 1, ширина) прошла поперёк матраса — автоматическая расстановка без утверждённой геометрии ненадёжна даже при аккуратных на вид координатах. Пока Олег не задал линии для конкретной модели, слайд «Размеры» для неё не публикуется (не добавляется в `GALLERIES`), даже черновиком.

**ПОДТВЕРЖДЁННЫЙ РАБОЧИЙ СПОСОБ для кроватей (Apogey, 14.09.2026) — генерация через kie.ai, а не собственные SVG-координаты Claude.** Разница с абзацем выше: Claude не пытается сам вычислить/расставить пиксельные координаты линий — это и было ненадёжно. Вместо этого запрос на kie.ai (`nano-banana-pro`, эндпоинт и опрос — как в общем разделе выше) просит саму модель посмотреть на фото и нанести выноски по словесному описанию. Одобрено пользователем на трёх карточках подряд (242 Кордова 2, 238 Алиса детская, 239 Квадро 1 — с первой попытки, без правок), включая нестандартную по форме «Алису» (низкая кровать с бортиком). Кровать у каждой не пострадала во всех трёх случаях.

- **Один товар — один запрос, кровать всегда в один слайд** (не два панели, как у диванов: у кровати нет сложенного/разложенного состояния).
- **Вход** — уже опубликованное реальное студийное фото без интерьера, `image_input: ["https://oleg2134234.github.io/mebel.hub/assets/<модель>/slide2_hero.jpg"]` (если такого фото в галерее ещё нет — сначала добавить его обычным слайдом, публиковать на GitHub Pages и подождать деплой, только потом звать kie.ai; ссылка должна реально открываться).
- **Если исходное фото фронтальное (анфас), а не 3/4** — сначала отдельным запросом к kie.ai повернуть кровать: промпт «пересоздай эту же кровать (тот же цвет, ткань, дизайн изголовья, пропорции), но камера смотрит с ракурса 3/4 (~30–40°), изголовье слегка сбоку, длинная грань уходит вглубь кадра, тот же студийный фон» (см. образец «241 Кордова 1 900» — поворот против часовой стрелки: изголовье оказывается слева-сверху кадра, изножье — справа-снизу). Результат сжать, опубликовать как временный `assets/<модель>/rotate-tmp.jpg` (коммит с пометкой WIP, дождаться деплоя), и только на нём наносить размерные линии по промпту выше. Подтверждено 14.09.2026 на «Кордова 3 900», «Кордова 4 900», «Лайт» — цвет, изголовье и (для «Лайт») деревянно-металлический каркас на ножках сохраняются корректно.
- **Тело запроса:** `{"model":"nano-banana-pro","input":{"prompt":"<промпт ниже>","image_input":["<URL>"],"aspect_ratio":"16:9","resolution":"2K","output_format":"png"}}`. `aspect_ratio: "16:9"` — под реальную пропорцию исходных фото этой партии (не «4:3» из старого общего примера).
- **Промпт (рабочий шаблон, EN, менять только числа и текст спального места):**
  ```
  Add dimension annotations directly onto this exact bed photo, without changing the bed itself in any way — keep its color, fabric texture, shape, proportions, camera angle and the plain background pixel-for-pixel identical. Overlay style: a thin dark charcoal (#20242b) line with a small solid filled circle at each end, and a dark rounded pill/capsule with bold white number centered on the line — a clean furniture-catalog dimension-line look. Add exactly three such lines plus one caption pill:
  1. A vertical line in the empty background to the left of the bed, from the top of its tallest point (headboard/back panel) down to floor level, with a pill reading "<высота, см>" (height, cm).
  2. A line following the bed's floor-level edge on the long visible side (the side receding away from the camera), starting at the bed's nearest floor corner and running to the far end of that side, with a pill reading "<длина, см>" (length, cm).
  3. A line following the bed's floor-level edge on the short visible side (the foot/end side), starting at that same nearest floor corner and running to the far end of that side, with a pill reading "<ширина, см>" (width, cm).
  4. One wide rounded dark pill centered at the bottom of the image with bold white text "Спальное место <ШxД> см".
  Every line and pill must stay entirely outside the bed's silhouette and its floor shadow, with only a small gap to the object — never overlapping or crossing the bed, mattress, or headboard. Do not draw leader/extension lines to the corners — only the three dimension lines themselves. Add no other text, watermark, logo or graphic. The rest of the image (bed, background) must remain exactly as in the source photo.
  ```
- **Числа** — те же правила, что и у диванов: сантиметры, запятая для десятых (`217,5`), из `PRODUCTS[].dims` (высота/длина/ширина корпуса) и `desc` (спальное место). Для товаров с диапазоном ширины (`"1390-1990"`) берём наибольший вариант, как для «Квадро 3»/«Кордова 1», и явно отмечаем это в подписи слайда: `«Габариты Д×Ш×В см (вариант 1800)»`.
- **Результат — PNG ~2750×1540, 4–5 МБ:** перед коммитом обязательно сжать до ширины 1600 px и сохранить в JPEG (`quality≈90`, скрипт как для остальных фото карточки — см. «Медиа и большие файлы»), только потом класть в `assets/<модель>/slide_dims.jpg` и подключать в `GALLERIES`.
- **Место в порядке слайдов** — сразу после «В интерьере», перед «Общий вид» (см. «Порядок слайдов в галерее»: «Размеры» — позиция 3, «Общий вид» относится к «Реальным фото» — позиция 6).
- Каждый результат утверждается пользователем по одному, до подключения в `GALLERIES` — процесс «по одному, ждать команду», не пакетно.

**АЛГОРИТМ ПОСТРОЕНИЯ РАЗМЕРНЫХ ЛИНИЙ (Олег, 10.09.2026 — применять всегда):**

1. **Построить проекцию дивана на пол** — уронить вертикали из ключевых углов дивана вниз до пола и соединить полученные точки. Это четырёхугольник-«след» дивана на полу с углами FL (перед-лево), FR (перед-право), BR (зад-право), BL (зад-лево).
2. **От грани проекции отступить наружу** на расстояние, которого достаточно, чтобы линия с плашкой встали чисто на полу, не задев диван и его приножевую тень.
3. **На этом отступе провести размерную линию строго параллельно грани проекции** (оба конца — на одинаковом перпендикулярном расстоянии от грани). Кружок на каждом конце, тёмная овальная плашка с белым числом в см по центру.
4. Линия длины и линия глубины выходят из общего переднего угла проекции — вместе читаются «уголком» (см. [tools/dims-style-ref.jpg](tools/dims-style-ref.jpg)).
5. Если фото почти фронтальное и грань глубины на полу вырождается — линию глубины не рисуем, значение пишем в подписи слайда (как высоту). Пример: «Бали 7.2» слайд «Размеры — сложенный вид».

**ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ К РАЗМЕРНЫМ ЛИНИЯМ (от 10.09.2026, соблюдать всегда и на каждой карточке):**

- **Все размерные линии — только на полу, ниже дивана, без пересечений с мебелью.** Никаких линий на уровне сиденья, спинки или «в воздухе».
- **Линия длины** идёт строго **параллельно переднему краю дивана**, с равномерным отступом от его ножек/основания по всей длине.
- **Линия глубины** идёт строго **параллельно боковой стороне дивана**, тоже по полу, с таким же по величине визуальным отступом от предмета, как у линии длины.
- **Ничто не должно наезжать на диван** — ни линии, ни круглые маркеры на концах, ни выноски, ни плашки с цифрами; в том числе не заходить на ножки дивана и на его тени.
- **Аккуратная геометрическая перспектива:** линии подчиняются перспективе пола на кадре, выглядят ровными; наклон линии совпадает с наклоном соответствующей грани в кадре, а не выравнивается по горизонтали/вертикали экрана.
- **Числа берём из данных карточки товара** (`PRODUCTS[].dims` и `desc` этого же id), переведённые в сантиметры; не выдумываем и не берём с другой модели.
- **Стиль инфографики сохраняем неизменным:** тонкие тёмные линии, круглые маркеры на концах, тёмные овальные плашки с белыми цифрами.

Практически: строить выноски детерминированным скриптом (PIL/SVG) по двум углам проекции измеряемой грани на пол `A`,`B` → концы линии `A+n*d`, `B+n*d` (одна и та же нормаль `n`, один и тот же отступ `d`), а не поручать нанесение нейросети. Перед подключением проверить обе панели глазами по всем пунктам выше. Пример правильного результата — карточка «Бали 7.2» (`tools/render-bali-7-2-dims.py`).

**Предпочтение пользователя от 05.09.2026 (уточнено):** применять связку imagegen для удаления фона и SVG для сборки двух панелей и точных векторных размерных выносок. После удаления фона обязательно сверять идентичность модели с исходником. Если исходный студийный кадр уже на белом фоне, можно встроить его в SVG без повторной генерации. Не называть генеративное удаление фона точной SVG-обтравкой. Генерация расходует лимит, работа Codex также расходует токены; не обещать обработку без затрат. Панели готовить отдельно и объединять вертикально; итоговый SVG допустим вместо JPEG.

**Стандарт для новых товаров по ссылке от пользователя:** когда пользователь даёт ссылку на карточку товара и просит добавить модель, без дополнительного напоминания создавать полную карточку каталога: переносить подтверждённые характеристики и источник, загружать все подходящие официальные изображения одной цветовой версии, исключать кадры другой расцветки/комплектации, приводить галерею к обязательному порядку, делать слайд «Размеры» из сложенного и разложенного кадров, добавлять «Преимущества и особенности», запускать аудит и показывать полную карточку на исправленном слайде. Если нужного кадра или достоверного размера нет, не выдумывать его и явно фиксировать пробел. **Обязательно проверять, что новый товар находится через квиз-подбор** («Подобрать за 30 сек» → тип товара → своя реальная ширина спального места/габариты) — добавление в `PRODUCTS`/`GALLERIES` не гарантирует, что подбор его найдёт (см. 14.09.2026: баг сравнения ширины корпуса вместо спального места прятал 5 новых кроватей и один старый товар).

**Одобренный результат 231 Бруно БД L от 05.09.2026:** пользователь одобрил обе панели на белом фоне. Фон в этих двух кадрах удалён imagegen, а SVG использован для компоновки и выносок; не называть это точной обтравкой исходных пикселей. Файлы: `dims-folded-white-v1.png`, `dims-unfolded-white-v1.png`, итог `slide_dims-v2.svg`.

Слайд размеров **не рисуем с нуля** — берём фото товара из его галереи, удаляем только окружение и добавляем выноски. Форму, цвет, фактуру, пропорции и ракурс дивана не меняем.

**Обязательное требование пользователя от 06.09.2026 — ракурс дивана на обеих панелях:**

- Слайд «Размеры» — это **всегда две панели**: вверху диван в сложенном виде, внизу — в разложенном.
- **Высоту на слайд не выносим.** На верхней панели — только длина и глубина (ширина) сложенного дивана, на нижней — только длина и ширина спального места. Значение высоты пишем текстом в подписи слайда (поле `desc`): «Габариты в сложенном виде: Д×Ш×В см…».
- **Диван на обеих панелях должен быть повёрнут на ~30–40° относительно фронта** — не прямое фронтальное фото, а лёгкая трёхчетвертная перспектива. При таком ракурсе видны и передняя нижняя грань, и уходящая вбок грань, поэтому линии длины/ширины и глубины ставятся корректно «уголком» (как в [tools/dims-style-ref.jpg](tools/dims-style-ref.jpg) и в карточках «Бали 5 ДК», «София 2 ДКУ»).
- **Прямое фронтальное («flat») фото для слайда размеров не годится** — на нём грань глубины вырождается, выноску глубины поставить некуда, линии выходят кривыми. Если в галерее только фронтальный кадр — сначала получить/сгенерировать трёхчетвертной ракурс той же модели (Nano Banana: повернуть предмет на 30–40°, всё остальное — цвет, ткань, форму, детали — сохранить пиксель-в-пиксель), и уже на нём строить слайд.
- **Верхняя и нижняя панель — один и тот же ракурс.** Диван вверху (сложенный) показываем под тем же углом, что и внизу (разложенный): трёхчетвертной вид спереди, камера немного выше и слева от дивана, направлена вниз на сиденье. Модель дивана — исходная, без изменений формы/цвета/фактуры.

**Обязательные требования пользователя от 04.09.2026 — для всех карточек, включая «Сандра 2 ДК»:**

- **Все числа на слайде «Размеры» — в сантиметрах.** Миллиметры переводим делением на 10 без потери точности: `2460 → 246`, `1065 → 106,5`, `945 → 94,5`, `2140 → 214`, `1500 → 150`. Десятичный разделитель — запятая; не округляем до целых.
- В капсулах оставляем только цифры (и десятичную запятую); в подписи слайда явно пишем «Размеры в сантиметрах» либо используем `см` после значений. Поле `PRODUCTS[].dims` остаётся в миллиметрах — это внутренний формат подбора, его не переводим.
- **Фон обязательно сплошной белый (`#FFFFFF`), непрозрачный:** только диван и размерные выноски, без комнаты, пола, ковра, посторонних предметов, градиентов и декоративных подложек. Прозрачность, шахматная сетка и серый фон не допускаются. Это правило действует для обеих панелей и пространства между ними.
- **Предпочтительное положение линий размеров — в нижней части дивана, от угла до угла его проекции на пол, на небольшом расстоянии от дивана.** Проекция на пол — только геометрический ориентир; сам пол и дополнительные контуры проекции не рисуем, фон остаётся белым.
- **Линия сверху допускается**, когда так компоновка получается чище: она должна быть строго параллельна соответствующей грани дивана, находиться полностью снаружи предмета и нигде не заходить на диван. Её концы по-прежнему соответствуют двум углам измеряемой грани и имеют одинаковый небольшой перпендикулярный отступ. Нельзя ставить линию сверху только ради свободного места, если она перестаёт соответствовать измеряемой грани.
- **Допустимый частный случай для разложенного дивана:** линию глубины спального места можно расположить сверху, за спинкой, как выноску над верхним контуром. Она должна идти параллельно направлению глубины спального места, располагаться целиком на белом поле, не касаться спинки и подлокотников и не пересекать диван. Нижнюю линию длины при этом можно оставить под передним краем спального места. Такое разнесение двух размеров считается правильным и помогает не создавать пересечение линий в одном нижнем углу.
- **Каждая размерная линия строго параллельна соответствующей стороне проекции на пол в перспективе кадра.** Оба её конца отстоят от соответствующих углов проекции на одинаковое небольшое перпендикулярное расстояние. Не выравниваем все линии по горизонтали или вертикали независимо от ракурса и не уносим выноски далеко от дивана.
- Геометрия строится по двум углам проекции измеряемой стороны на пол `A` и `B`: концы выноски — `A + n*d` и `B + n*d`, где `n` — единичная нормаль к стороне проекции, `d` — один и тот же небольшой внешний отступ. Координаты углов проверяем по фото; линии и цифры предпочтительно наносим детерминированно, а не поручаем нейросети угадывать их положение.
- Линия находится снаружи дивана, не пересекает его; на концах одинаковые кружки, капсула с числом — по центру. Отступы выносок в пределах панели визуально согласованы. Размеры спального места относятся к поверхности для сна, не к наружным краям подлокотников.
- Перед подключением проверяем обе панели: фон белый, предмет не изменён, значения в сантиметрах; нижние линии привязаны к углам проекции на пол, верхние — к углам соответствующей грани; отступы небольшие и равные; линии параллельны измеряемым граням, полностью находятся снаружи предмета, не заходят на диван; цифры и выноски не обрезаны.

**Структура слайда — фиксированная.** Итоговый «Размеры» — это две панели, склеенные вертикально:

- **верхняя панель — диван в сложенном виде**, выноски только длина × глубина (высоту на слайд не ставим — она в подписи);
- **нижняя панель — диван в разложенном виде**, с размерами спального места (длина × ширина).

Панели готовим **по отдельности**: сначала каждый кадр отдельно — с нанесёнными выносками и цифрами, — и только потом склеиваем оба в один слайд `assets/<модель>/slide_dims.jpg`. Порядок панелей менять нельзя: сложенный вид всегда сверху, разложенный всегда снизу. Если разложенного кадра у карточки нет — сначала добавить его обычным слайдом в галерею (см. п. 1), слайд «Размеры» без нижней панели не выпускаем.

1. **Исходники — те же кадры, что уже в галерее этой карточки:** берём фото сложенного вида (для верхней панели) и разложенного вида (для нижней) прямо из слайдов товара. Новые кадры не подбираем и диван заново не генерируем — слайд «Размеры» должен быть теми же фото, что видит покупатель в остальной галерее. Если в карточке нет разложенного вида — доснять/взять у производителя (mebvektor.ru) и сначала добавить его обычным слайдом в галерею.
2. **Подготовка и выноски.** Сначала удаляем окружение исходного фото, сохраняя сам диван без изменений. Затем наносим линии по геометрическому правилу выше (линия + кружки на концах + капсула). Сгенерированные выноски без проверки геометрии не принимаем. Старые скрипты и сервисные примеры ниже не отменяют эти требования. Ключ Kie.ai: `<домашняя папка>\.claude\kie_api_key.txt` (у каждой машины свой).
   - Эндпоинт: `POST https://api.kie.ai/api/v1/jobs/createTask`, `Authorization: Bearer <key>`.
   - Тело: `{"model":"nano-banana-pro","input":{"prompt":"…","image_input":["<URL реального фото>"],"aspect_ratio":"4:3","resolution":"2K","output_format":"png"}}`.
   - Опрос: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId=…` до `data.state == "success"`, затем ссылка в `JSON.parse(data.resultJson).resultUrls[0]` (живёт ~24 ч — скачивать сразу).
   - Фото отдаём по URL с GitHub Pages: `https://oleg2134234.github.io/mebel.hub/assets/…`.
   - Рабочий скрипт-образец: `tools/gen-dims-slide.ps1` (на примере Адель 4 КК).
3. **Промпт для удаления фона** (EN, суть): «Remove only the surrounding room, floor and props; isolate the exact sofa on a solid opaque pure white background (#FFFFFF). Preserve the sofa pixel-for-pixel: color, fabric texture, proportions, silhouette, camera angle and all details. Do not redesign, redraw, duplicate or simplify the sofa. No text, no dimensions, no transparency, no checkerboard pattern, no gray background, no gradients». Для одной панели — ровно один предмет. После удаления фона сверяем с исходником. Оформление выносок: тонкая тёмно-графитовая линия, кружок на каждом конце, тёмная капсула с белым жирным числом в сантиметрах. Эталон графики — [tools/dims-style-ref.jpg](tools/dims-style-ref.jpg); новые требования к фону, единицам и геометрии имеют приоритет над старым эталоном.
4. **Числа** — из подтверждённых данных производителя, на слайде обязательно в сантиметрах. Для трансформера: сложенный вид — длина/глубина (высоту на слайд не выносим, пишем в подписи); разложенный — длина спального × ширина спального. Неподтверждённые значения не придумываем.
5. **Сборка слайда — своими силами** (не в нейросети): склеить панели вертикально, сложенный вид сверху, разложенный снизу, обязательно сплошной непрозрачный белый фон `#FFFFFF`, небольшой белый зазор. Уменьшить до ~1200 px по ширине, сохранить как `assets/<модель>/slide_dims.jpg`. При смене имени обновить ссылку в галерее. Исходник сохранять до проверки новой версии.

### Как делать слайд «Уют» (обязательный процесс)

**ПОДТВЕРЖДЕНО 14.09.2026 (20 кроватей: старые + вся партия Apogey) — генерация через kie.ai по той же схеме, что и «Размеры».** Слайд «Уют» (позиция 2 в галерее, сразу после «В интерьере») — жанровая сцена: тот же товар, человек и питомец, тёплый вечерний свет. Эталон стиля — уже существовавшие слайды `slide_cozy.jpg` у «Квадро 2», «Квадро 3», «Кордова 1» (тёплая лампа, вязаный плед, книга, кот/собака на пледе).

- **Вход** — тот же реальный студийный кадр без интерьера, что и для «Размеры» (`slide2_hero.jpg`), по живому URL с GitHub Pages.
- **Промпт (EN, суть, менять цвет/питомца под конкретный товар):** «Take the exact bed from this photo and place it into a warm, cozy evening bedroom scene… This bed's upholstery is `<цвет с фото>` — that exact color must be preserved with zero shift… more important than the scene styling… Add a person in cozy loungewear sitting relaxed against the headboard reading a book, and a `<cat/dog>` resting on the foot of the bed on a knit throw blanket that contrasts with the upholstery… warm bedside lamp, cozy decor, plants, books… Before finishing, double-check the upholstery is still exactly `<цвет>`».
- **Цвет в промпте — только с реального фото товара**, не из `desc` (см. правило идентичности выше — на 14.09.2026 это несовпадение подвело несколько раз).
- **Детская кровать (title содержит «детская»)** — вместо взрослого в сцене ребёнок 6–8 лет с книжкой-картинкой, игрушки на полке вместо взрослого декора. Взрослый на «детской» кровати в кадре выглядит неуместно для покупателя, даже если сам товар не пострадал.
- **Линия «НС» (низкое изголовье)** — явно указываем в промпте, что изголовье низкое и должно остаться низким, и что человек облокачивается на отдельную подушку, а не на само изголовье (иначе модель имеет тенденцию рисовать изголовье выше, чем на самом деле — как и с размерными линиями, текстовый контроль точной геометрии у kie.ai ненадёжен; 2 из 4 карточек «НС» всё равно получили изголовье повыше реального даже с этой оговоркой — цвет и материал при этом были верны, решили опубликовать как есть, не гонясь за идеалом на неограниченное число попыток).
- **Результат** — сжать до ширины 1600 px JPEG, положить в `assets/<модель>/slide_cozy.jpg`, подключить в `GALLERIES` сразу после «В интерьере».
- Показываем полную карточку в браузере на исправленном слайде до публикации — то же обязательное правило, что и везде.

### Как делать видео облёта (360°)

Модель — **`kling/v2-5-turbo-image-to-video-pro`** на Kie.ai (дешевле и качественнее Wan/Seedance для этой задачи: ~42 кредита за 5-сек клип).

1. **Вход — ДВА реальных кадра** товара: `image_url` (стартовый — вид спереди 3/4) и `tail_image_url` (конечный — вид сзади). Оба — официальные фото производителя из `assets/<модель>/` (front + back), по URL с GitHub Pages.
2. **Тело запроса:** `{"model":"kling/v2-5-turbo-image-to-video-pro","input":{"prompt":"…","image_url":"<front>","tail_image_url":"<back>","duration":"5"}}`. Эндпоинт и опрос — как у слайдов размеров (`jobs/createTask` → `jobs/recordInfo` → `resultJson.resultUrls[0]`).
3. **Промпт (EN, суть):** плавный непрерывный облёт камеры на 180° по часовой, старт ровно на первом кадре, финиш ровно на последнем; предмет **неподвижен и не деформируется — сохранить цвет, фактуру, пропорции**; фон ровный светло-серый студийный, без людей и зума.
4. **Полный 360 — из двух клипов:** A (перед→зад) + B (зад→перед, кадры местами), склеить своими силами. Перед и зад берутся с реальных фото, «плавает» только на боковых четвертях. ~84 кредита на диван.
5. Готовый mp4 класть в `assets/<модель>/video_orbit.mp4`, слайд в галерее — тип `video`, имя «Видео: облёт», в блоке видео (после «Видео: раскладка»).

## Экраны

SPA без роутера: `showScreen(id)` переключает `<section>`. Экраны: `homeScreen`, `catalogScreen`, `fabricScreen` (подбор ткани), `tipsScreen` (советы продавцу), `supplierScreen` (категории тканей поставщиков). Плюс модалки: `quizOverlay` (подбор) и `galleryOverlay` (галерея фото товара).

Ссылки на страницы коллекций у поставщиков тканей — в объекте `FABRIC_LINKS` (`{бренд: {название: url}}`), рендерятся `fabricNameHTML()`; названия без записи в `FABRIC_LINKS` просто показываются текстом без ссылки — это штатное поведение, а не ошибка. Сайты поставщиков переделывают структуру каталога без предупреждения — **14.09.2026 у бренда ARBEN 6 из 11 ссылок оказались битые (404)**, удалены из таблицы. Раз в какое-то время стоит перепроверять весь `FABRIC_LINKS` (`curl -s -o /dev/null -w "%{http_code}" -L -A "Mozilla/5.0 ..." <url>` по каждой, некоторые поставщики блокируют запросы без user-agent) и либо чинить через поиск на сайте поставщика, либо просто убирать битую ссылку, оставляя название текстом.

## Оформление

- Тема светлая/тёмная — **только через CSS**: `@media (prefers-color-scheme: dark)` + переопределения `:root[data-theme="..."]`. Следует за настройкой ОС, ручного переключателя нет.
- Цвета — через CSS-переменные в `:root` (`--bg`, `--surface`, `--ink`, `--accent`, `--gold`, ...). Хардкод цветов в правилах не добавляем — только переменные.
- Весь текст интерфейса — русский. Кавычки — «ёлочки», тире — длинное (—).

### Шрифт

- **Один шрифт на весь проект — Inter**, самохостинг в `assets/fonts/` (2 файла woff2, variable-начертание: `inter-latin-wght-normal.woff2` + `inter-cyrillic-wght-normal.woff2`). Внешних CDN нет, работает офлайн. Заголовки и текст — один и тот же Inter (никаких serif).
- Базовый кегль — на `html`: `font-size: clamp(1rem, 0.95rem + 0.3vw, 1.15rem)`. Отталкивается от системного размера шрифта устройства, чуть подрастает на широких экранах, ограничен сверху. **Весь макет свёрстан в `rem`** — размер меняется этим одним правилом, поштучные `font-size` не трогаем.

### Адаптивность

Дизайн резиновый, но **структура и восприятие едины на всех экранах** — на широком мониторе не должно превращаться в другой макет.

- Каталог `.grid`: `repeat(auto-fill, minmax(240px, 1fr))`, `max-width: 1240px` — столбцы добавляются сами по ширине экрана, карточка везде одинаковая. Шапка каталога сжимается при прокрутке (класс `.compact` на `header.site`).
- Модалки (`.quiz-modal`, галерея): `max-height` в `vh` + внутренняя прокрутка. Должны помещаться в экран без вылезания.
- Единицы — `rem` / `%` / `clamp()` / `vw`. Фиксированные `px` только для мелочей (толщина рамок, радиусы).

### Иконки в рамке-плитке

Правило для всех кнопок вида «иконка + подпись в квадратной плитке» (типы мебели в квизе, условия ткани, плитки на главной):

- **Иконка занимает ~3/4 плитки.** Белое/светлое поле плитки — тонкая рамка вокруг иконки, не наоборот.
- Размер иконки задаётся **в процентах от плитки** (`width: ~74–80%`), чтобы иконка росла вместе с плиткой на широких экранах.
- Плитка квадратная (`aspect-ratio: 1 / 1`), маленькое внутреннее поле (`padding: ~4–5%`).

## Контент

Фото товаров — **реальные снимки конкретной модели**, не рендеры и не сток. Тексты про материалы и гарантию — только проверенные фактические цифры (плотность ППУ, сроки гарантии и т.п.), без маркетинговых преувеличений.

## Медиа и большие файлы

**Правило 2 из обязательных.** Репозиторий и так раздут (~500 МБ истории), поэтому:

- **Ничего тяжелее ~5 МБ в git не коммитим.** Особенно видео.
- **Папки `reels/`, `social/`, `story/`, `video/` — не для git.** Это исходники для соцсетей, сайт их не открывает. Хранить во внешнем месте (облачный диск, отдельное хранилище). В идеале этих папок в репозитории быть не должно — вместо них `README` со ссылкой на хранилище. Новые SMM-ролики сюда не добавляем.
- **Видео, которые нужны сайту** — только `assets/<модель>/video_*.mp4` (схемы раскладки, короткие облёты). Цель — **≤ 3 МБ**, потолок **5 МБ** на файл. Больше — пережать перед коммитом: H.264, до 720p, пониженный битрейт, без звука если он не нужен.
- **Фото** — JPG, разумное разрешение (ширина ~1600 px достаточно для слайда), с компрессией. PNG только для иконок с прозрачностью.
- Если по-настоящему большой файл всё же обязан быть в репозитории — только через **Git LFS**, не обычным коммитом.

Проверка перед `sync.ps1 end`: `git diff --cached --stat` — если видишь мегабайты, останови коммит и разберись.

## Предпросмотр локально

Node/Python на машине нет — поднимаем статику на встроенном в Git perl:

```bash
perl tools/serve.pl . 8791
```

Затем открыть http://localhost:8791/ . Просто открыть `index.html` файлом (`file://`) нельзя — ломаются относительные пути к `assets/` и не грузятся шрифты.

## Деплой

Пуш в `main` → GitHub Pages публикует автоматически за 1–2 минуты. Отдельного шага сборки нет.

## Откат обновлений

Два уровня отката:

1. **Быстрый, без git.** `sync.ps1 end` / `sync.sh end` перед каждой отправкой кладёт zip исходников в `backups/` (`source_ГГГГ-ММ-ДД_ЧЧММ.zip`) — `index.html`, `CLAUDE.md`, скрипты, `tools/`, `assets/fonts`, `assets/brand`. Хранятся 15 последних, папка `backups/` в `.gitignore`. Сломалось после обновления — распаковал нужный zip поверх, открыл предпросмотр, проверил. Тяжёлое медиа в zip не входит (оно меняется редко).
2. **Полный, через git.** Вернуть один коммит: `git revert <хеш>`. Вернуть конкретный файл к прошлому состоянию: `git checkout <хеш> -- index.html`. Список коммитов: `git log --oneline`. После отката — снова `sync end`.

## Синхронизация между компьютерами

Проект ведётся с двух компьютеров **по очереди** (например: утром — компьютер A, вечером — компьютер B). Одновременно на обоих не работаем.

**Единственный источник правды — GitHub.** Между машинами передаём только через `git push` / `git pull`.

### Чего нельзя делать

- ❌ Копировать папку проекта вручную (флешка, «перекину архив»).
- ❌ Держать папку в OneDrive / Dropbox / Google Drive / Яндекс.Диске — они повреждают `.git` при частичной синхронизации.
- ❌ Работать, не сделав `sync start` в начале.
- ❌ Заканчивать день, не сделав `sync end`.
- ❌ Держать проект в `Downloads` (некоторые чистилки его подчищают) — перенеси в стабильную папку, например `C:\dev\mebel.hub`.

### Разовая настройка на каждой машине

Один раз на компьютере, где проекта ещё нет:

```bash
git clone --depth 1 --single-branch --branch main https://github.com/oleg2134234/mebel.hub.git
cd mebel.hub
git config pull.rebase true
```

`--depth 1` — поверхностный клон без истории (история ~500 МБ, полный клон качается очень долго). Для правки `index.html` и ассетов история не нужна.

Если позже `git pull` заругается на `shallow` — один раз выполнить `git fetch --unshallow` (это разово докачает историю).

### Ежедневный ритуал (обе машины одинаково)

**1. Начинаешь работу — подтяни изменения с другой машины:**
```powershell
.\sync.ps1 start
```
Git Bash: `./sync.sh start`

Делает `git pull --rebase --autostash`. После «Готово. Репозиторий актуален» — можно править файлы.

**2. Закончил работу — сохрани и отправь:**
```powershell
.\sync.ps1 end "что сделал за сессию"
```
Git Bash: `./sync.sh end "что сделал за сессию"`

Делает: zip-бэкап в `backups/` → `add -A` → `commit` → ещё раз `pull --rebase` → `push`. После «Отправлено» — можно выключать компьютер.

Правило простыми словами: **сел работать → `start`. Встал из-за компьютера → `end`.** Каждый раз, на любой из машин.

### Передача работы с машины 1 на машину 2

Чтобы вечером на машине 2 сразу продолжить с того же места:

1. **Машина 1, конец работы:** `.\sync.ps1 end "…"` и дождись строки **«Отправлено»**. Пока её нет — работа не ушла на GitHub, машина 2 её не увидит. Не закрывай терминал до этой строки.
2. **Машина 2, начало работы:** `.\sync.ps1 start`. Через пару секунд там ровно то состояние, на котором закончила машина 1 — можно править дальше.

Между этими двумя шагами ничего вручную переносить не нужно. Единственное условие — на шаге 1 был интернет и `push` прошёл. Если интернета не было, машина 1 остаётся «ведущей»: садиться за машину 2 нельзя, пока не сделаешь `push` с машины 1.

Мини-чеклист смены: `end` → «Отправлено» → пересел → `start` → «Актуален» → работаешь.

### Типичные ситуации

| Ситуация | Что делать |
|---|---|
| Забыл `start`, уже что-то наменял | Просто запусти `.\sync.ps1 end "…"` — внутри он сам подтянет чужое через `--rebase` и наложит твоё поверх. |
| `start` пишет про конфликт | Открой файл из сообщения, найди `<<<<<<<` / `=======` / `>>>>>>>`, оставь нужный вариант, `git add <файл>`, затем `git rebase --continue`. Конфликты возможны только если обе машины правили один и тот же кусок `index.html`. |
| `end` пишет `push` отклонён | Значит на другой машине был свежий `end`. Запусти `.\sync.ps1 end "…"` ещё раз — он сделает `pull --rebase` и повторит `push`. |
| Совсем запутался в состоянии git | `git status`, потом спроси Claude Code — не делай `reset --hard` наугад, потеряешь работу. |
| Работал на машине без интернета | Коммить локально (`git commit`), `push` сделаешь, когда появится сеть. На другую машину не пересаживайся, пока не запушил. |

### Долгосрочно

История репозитория тяжёлая из-за бинарных ассетов и старых тегов `catNN`. Стоит один раз: удалить ненужные теги, вынести медиа в Git LFS или во внешнее хранилище, почистить историю (`git filter-repo` / BFG, затем `--unshallow`-переклон на второй машине). После этого синхронизация станет мгновенной. Пока не сделано.

## Что дальше (открытые задачи)

_Этот раздел — «память» между сессиями и машинами. Обновляй его по ходу работы._

Последняя сессия (02.09.2026): сплошной покадровый аудит **всех 32 карточек диванов
(221 слайд)** и первая волна правок. Проверялись три вещи: один ли диван на всех
слайдах карточки, верны ли числа и выноски на слайде «Размеры», соблюдён ли
фиксированный порядок слайдов.

Отчёт по аудиту (артефакт, приватный): <https://claude.ai/code/artifact/cce93e1c-a384-49cd-b7b7-9cb38645bd29>

### Сделано в этой сессии

Правки в `index.html` (имена, подписи, порядок) и новые слайды «Размеры».

- **Порядок слайдов приведён к правилу во всех 32 карточках** (было 16 нарушений).
  «Практичность» теперь последняя везде: id 9, 10, 42, 43, 203, 207, 208.
  Реальные фото подняты до разложенного вида: id 7, 17, 58, 228.
- **229 Лортон-2 ДКУ-О** — «Вид спереди» и «Вид сзади» показывали друг друга,
  файлы поменяны местами; первый слайд переименован «Общий вид» → «В интерьере».
- **43 Марк ДК** — слайд «Раскладка» показывал диван сбоку в сложенном виде,
  переименован в «Вид сбоку».
- **203 Финка ДК** — слайд «Комфорт» переименован в «Положение спинки — полусидя»
  (это промежуточное положение механизма «Книжка»).
- **58 Бали с оттоманкой** — подпись слайда «Размеры» исправлена: было
  «336×165×165 см», стало «336×165×90 см» (165 — это глубина, а не высота).
- **220, 221, 223 Джой** — «Общий вид (студия)» → «Общий вид» (кадр не студийный).
- **222, 224, 225, 226 Джой** — первый слайд «Общий вид» → «В интерьере».
- **Слайды «Размеры» пересобраны:** 231 Бруно БД L (создан с нуля, вручную),
  205 Мартин ДКУ-П, 58 Бали с оттоманкой, 220 Джой 1, 223 Джой 4, 225 Джой 6
  (через Nano Banana, диван не менялся). Пути и числа — в блоке A.
- **5 Остин 3 ДК** — описание переписано: прямой диван, каркас из дерева,
  ортопедические латы, декор МДФ. Замена слайдов углового дивана — в блоке B.
- **229 Лортон-2 ДКУ-О** — слайд «Размеры» переоформлен в общем стиле каталога
  (тонкая линия, кружки, тёмная капсула) вместо толстых стрелок.
- **221 Джой 2 ДК** — слайд «В интерьере» перекрашен из оранжевого в изумрудный;
  последний слайд «Общий вид» удалён из галереи.
- **7 Бали 5 ДК** — слайды «В интерьере» и «Уют» перекрашены из бежевого
  в терракоту под реальные фото товара (комната и люди не тронуты).
- **206 София 2 ДКУ** — в слайд «Размеры» добавлена нижняя панель: разложенный
  вид со спальным местом 273×136 (центральная секция выдвинута вперёд).
  Разложенный вид сгенерирован Nano Banana из студийного фото, выноски —
  свои (`scratchpad/audit/comp206.ps1`). Убрана текстовая плашка с верхней панели.
- **228 Адель 2 ДК** — слайд «Размеры»: верхняя панель 220×105 без выноски
  высоты, нижняя 197×197 (спальное квадратное, подтверждено). Nano Banana.
- **207 Финка 3 ДК** — в слайд «Размеры» добавлена нижняя панель: реальное
  студийное фото разложенного вида (slide4_unfold.jpg) + выноски 197×140.
  Текстовая плашка с верхней панели убрана (`scratchpad/audit/comp207.ps1`).
- **77 Адель 4 ДК** — модель бывает в двух размерах спального места (197×197
  и 197×152). Слайд оставлен на 197×152, снизу добавлена жирная подпись
  «Также выпускается со спальным местом 197 × 197 см»; в описании — оба размера.
- **231 Бруно БД L** — пробовали пересобрать через Nano Banana: диван и шоурум
  сохранились, но выноски легли по диагонали (217 поперёк матраса, 308 через
  пол). Ручной вариант точнее — оставлен он. Больше не трогать.

Точка отката: коммит перед правками, плюс zip в `backups/`.

### Не сделано — по приоритету

Новый аудит карточек от 03.09.2026 сохранён в `AUDIT_CURRENT.md`.
Автоматическая проверка — `tools/audit-catalog.js`, обзорные листы 32 диванов —
`audit-output/contact-sheets/`.

Закрыто по результатам нового аудита:

- **227 Адель 4 МДК (04.09.2026)** — на слайде «Размеры» исправлено
  положение выносок спального места по указанию пользователя: `120` поставлен
  слева от разложенного дивана, `197` — снизу параллельно дивану. Размер
  сложенного дивана `141` проверен и оставлен без изменения. Оба исходных
  изображения дивана сохранены без перерисовки. Файл:
  `assets/adel-4-md/slide5_dims-v3.png`; скрипт:
  `tools/clean-adel-4-md-dimensions.ps1`.
- **77 Адель 4 ДК (04.09.2026)** — слайд «Размеры» заменён на
  `assets/adel-4-dk/slide_dims-v2.png`. Оба исходных дивана сохранены без
  перерисовки; размер `152` перенесён в левую нижнюю часть разложенного дивана
  и поставлен параллельно его проекции на пол. Удалены две лишние текстовые
  плашки и неверная правая вертикальная выноска, почти белая JPEG-подложка
  приведена к чистому `#FFFFFF`. Скрипт: `tools/clean-adel-4-dk-dimensions.ps1`.
- **43 Марк ДК (04.09.2026)** — `assets/mark-dk/slide5_dims-v2.png` подключён
  как явно одобренное пользователем исключение. В версии белый фон, нет лишней
  плашки, в капсулах только сантиметры: `90`, `110`, `233`, `156`, `205`.
  Для остальных карточек это исключение не действует.
- **232 Сандра 2 ДК (04.09.2026)** — карточка и её 10 медиа перенесены из актуального
  каталога GitHub в локальный проект без замены остальных карточек. Слайд
  `assets/sandra-2-dk/slide_dims.jpg` заменён: белое поле, сантиметры `246`, `106,5`,
  `214`, `150`; описание слайда тоже в сантиметрах. Поле `dims` сохранено в мм.
  Геометрия выносок воспроизводится `tools/render-sandra-dimensions.ps1` по
  `assets/sandra-2-dk/dims_base.png`. Для обоих концов каждой линии используется
  одинаковый перпендикулярный сдвиг; скрипт проверяет параллельность и отсутствие обрезки.
  Подложка подготовлена ранее через imagegen; выноски нанесены программно.
  Исходный слайд сохранён в `audit-output/sandra-review/slide_dims-original.jpg`.
- **205 Мартин ДКУ-П (04.09.2026)** — проверен и подключён ранее подготовленный
  `assets/martin-dku-p/slide1_hero-v2.png` без встроенных надписей и бейджей:
  главное изображение `IMAGES[205]` и первый слайд галереи. Исходный JPG сохранён.
- **121 Квадро 3** — «Общий вид (студия)» и «Вид спереди» перенесены перед
  финальной «Практичностью».
- **204 Регина 4.1 Кресло** — «Практичность» перенесена в конец галереи.
- **174 Нова 2 Матрас** — добавлено нейтральное описание только на основе
  подтверждённых размеров.
- **9 Атлант 2 ДК** — из первого интерьерного изображения удалено вшитое
  название модели; подключена обратимо сохранённая версия
  `assets/atlant-2-dk/slide6_hero_interior-v2.png`.
- **46 Мартин ДКУ-О** — из первого интерьерного изображения удалено вшитое
  название модели; подключена версия
  `assets/martin-dku-o/slide7_hero_interior-v2.png`. Слайд «Размеры»
  переделан: верхний блок 265×167×96, спальное место 213×130; подключён
  `assets/martin-dku-o/slide_dims-v2.png`.

Следующий контентный приоритет:

1. Серия **Джой 220–226** — реальные фото, разложенный вид и практичность.
2. Удаление вшитого текста и белых полей у **5, 9, 46, 201, 202, 205,
   220–226**.
3. Дополнение коротких галерей **201, 205, 206, 231**.
4. Оптимизация видео тяжелее 5 МБ.

**A. Слайды «Размеры» — переделать изображение.**
Процесс — раздел «Как делать слайд Размеры». Nano Banana (`nano-banana-pro` на
Kie.ai) отлично справляется, когда фон студийный: промпт «верни фото без
изменений, только убери старые выноски и нанеси N новых на полу вдоль граней».
Рабочий скрипт запроса — `scratchpad/audit/kie.pl` (в этой сессии), шаблон
промпта — там же `p*.txt`. Для нижней панели модульных диванов формулировку
пришлось усиливать: явно писать «линия лежит на полу вдоль нижней грани
матраса, не парит над матрасом и не висит в воздухе».

Сделано в этой сессии (id → путь):
- **231 Бруно БД L** — слайда не было, собран вручную (шоурумный фон):
  `assets/bruno-bd-l/slide_dims.jpg`, добавлен в галерею. 308×110 / 217×170.
- **205 Мартин ДКУ-П** — `assets/martin-dku-p/slide_dims.jpg`. 333×185 / 270×130.
  (подтверждено 02.09: верхняя панель 333×185 без высоты, спальное 270×130)
- **58 Бали с оттоманкой** — `assets/bali-5-dk/slide_dims.jpg`. 336×165 / 300×160.
- **220 Джой 1 ДКУ-О** — `assets/joy-1-dku-o/slide3_dims.jpg`. 278×180 / 174×198.
- **223 Джой 4 ДК** — `assets/joy-4-dk/slide3_dims.jpg`. 314×133 / 174×198.
- **225 Джой 6 ДКУ-О** — `assets/joy-6-dku-o/slide3_dims.jpg`. 341×180 / 174×198.

Осталось:

| id | Модель | Что не так |
|---|---|---|

Плюс мелочь: высота не показана у **201, 202, 220–226**; текстовые плашки
«Спальное место …×… см» вшиты в слайд у **4, 10, 18, 42, 43, 77, 203, 208, 230**
(правило — только цифра, без букв).

**B. Цвет и состав слайдов.**

- **221 Джой 2 ДК — привести к изумрудно-зелёному (уточнено 02.09).**
  Слайд «В интерьере» (`assets/joy-2-dk/slide_interior_new.jpg`) — оранжевый,
  перекрасить в изумрудно-зелёный под остальные слайды карточки. Последний
  слайд «Общий вид» (тёмно-бирюзовый рендер) — удалить из галереи.
  Промпт перекраса готов: `scratchpad/audit/p221.txt`.
- **5 Остин 3 ДК — заменить слайды углового дивана (уточнено 02.09).**
  Это ПРЯМОЙ диван, не П-образный. Каркас из дерева, ортопедические латы,
  функциональные подлокотники, широкое спальное место, декор МДФ, механизм
  «Тик-так». Описание и dims (3460×1170×980) уже поправлены — глубина 1170
  для прямого корректна, вопрос из блока C снят.
  Осталось: слайды `slide_hero_interior.jpg` («В интерьере») и
  `slide_hero_real.jpg` («Общий вид») показывают П-образный диван с двумя
  оттоманками — заменить на прямой. Реф и точные характеристики:
  https://mebvektor.ru/products/divan-ostin-3-dk-v-3-h-tkanyah
  Прямые слайды в галерее уже есть: Размеры, Ракурс 3/4, Вид сзади, Раскладка,
  Спальное место, Бельевой ящик. Нужны новые: интерьерная визуализация
  прямого дивана и «Общий вид» спереди. Угловые кадры — в отдельную модель
  «Остин ДКУ», если её заведут.
- **Серия Джой (220–226) — только рендеры**, реальных фото нет ни у одной модели,
  кроме «В разложенном виде» у Джой 7. Правило проекта требует реальных снимков.
- **Вшитый в картинку текст** (название модели, бейджи, белые поля): id 9, 46, 5,
  201, 202, 205 и «УСТРОЙСТВО» на слайдах «Разрез» у 220–226.
- Не хватает слайдов: «Уют» у **205, 231**; «Разрез» у **205, 230, 231**;
  реальных фото и разложенного вида у **201, 220–225**.

**C. Уточнить у производителя (mebvektor.ru) — без этого не чинить.**

- **Адель 2 ДК (228)** — РЕШЕНО 02.09: спальное место 197×197 см (квадрат).
  Слайд: верх 220×105 без высоты, низ 197×197. В описании убрать «/1520».
- **Серия Джой (220–226)** — спальное место по каждой модели: сейчас у всех семи
  одинаковое 1740×1980 при длине корпуса от 242 до 377 см. Плюс что означает
  вариант в скобках у Джой 3 и Джой 6: «1740×1980(1590×1980)».
- **Лортон-2 ДКУ-О (229)** — механизм и наполнение (в описании так и висит
  «уточняются у производителя»).
- **Мартин ДКУ-П (205)**, **Финка 1 ДК (208)**, **Финка 3 ДК (207)**,
  **Лотос 2 ДКУ (10)** — механизм раскладывания не указан в описании.

**D. Хвосты прошлых сессий.**

1. **«Подбор по цвету» — переделать сетку свотчей в единое цветовое поле.**
   Вместо 16 отдельных квадратов — один прямоугольник с плавным переходом
   цветов (по диагонали тёмное→светлое, плюс оттенки от тёплых к холодным),
   без видимых границ. Названия цветов — поверх поля. Клик по полю → выбирается
   ближайший из `FABRIC_SWATCHES` → `renderFabricResults(idx)` (эта функция уже
   есть, менять не нужно). Контейнер сейчас — `#fabricSwatchGrid` на экране
   `fabricScreen`, наполняется `swatchGridHTML()`.

2. **Разметка 🐾/🤧 по коллекциям ткани — заменить заглушку на реальные данные.**
   Сейчас `fabricSuitability(name)` считает пригодность из хеша имени — это
   временно. Нужен ручной список: каждой коллекции (ШТОРМ, СОТБИ, …
   в `FABRIC_CATEGORIES`) проставить `pets` и `allergy` = 0/1/2. После этого
   кнопки «Дома есть животные» / «Есть аллергия» сделать полноценным фильтром
   (сейчас только подсвечивают неподходящее через класс `.flt-pets`/`.flt-allergy`).

3. **Видео облёта 360° для Бали 5 ДК.** Обе половины (Kling) генерировались
   28.08, ссылки Kie.ai живут ~24 ч и давно протухли — половины надо
   генерировать заново. **ffmpeg на машине нет**: скачанный тогда
   `ffmpeg.zip` (26 МБ) оказался обрезанным и не распаковывается. Поставить
   портативный билд заново — он же нужен для сжатия тяжёлых видео.
   Готовый ролик → `assets/bali-5-dk-standart/video_orbit.mp4`, слайд в галерею id 7.

### Как быстро воспроизвести аудит

Контактный лист по карточке (все слайды одной модели в одной картинке) собирается
скриптом на PowerShell + System.Drawing — Node/Python на машине нет. Логика:
прочитать `PRODUCTS`/`GALLERIES` из `index.html` в JSON, для каждой карточки
категории `sofa` сложить миниатюры слайдов сеткой 4 в ряд с подписями
`[индекс] имя` и именем файла. Порядок слайдов отдельно проверяется скриптом
по рангам из раздела «Порядок слайдов в галерее».


## Доработка каталога с 05.09.2026

План: [docs/catalog/PLAN.md](docs/catalog/PLAN.md). Журнал: [docs/catalog/CHANGELOG.md](docs/catalog/CHANGELOG.md). За один шаг дорабатываем один ID, показываем полную карточку и фиксируем результат. Данные аудита — снимок, а не утверждение о состоянии после исправлений.

### Крупный товар в кадре

- Товар — главный объект. На общем виде занимает ориентировочно 75–90% полезной ширины кадра, показан целиком; подлокотники, ножки и оттоманка не обрезаны.
- Убираем лишнее окружение кадрированием только при сохранении предмета. Не растягиваем и не перерисовываем мебель. Малое разрешение не исправляем выдуманными деталями.
- В галерее используем contain, сохраняем пропорции; проверяем крупный экран и узкую панель/телефон. Текст и декор не закрывают мебель.

### Новый слайд «Преимущества и особенности»

- Точное название: «Преимущества и особенности». После хранения, перед финальной «Практичностью»; если её нет — последний.
- Крупное неизменённое фото именно этой модели и 2–4 коротких индивидуальных факта. Каждый факт объясняет пользу: особая оттоманка, подушки, хранение, конструкция, подтверждённые опции.
- Только видимые на фото или подтверждённые источником свойства. Не выдумывать нагрузку, медицинские свойства, гарантию, «антикоготь», состав и комплектацию.
- Допускается адаптивный HTML/CSS-слайд type: features с исходным фото в src и features: [{title,text}]. Это сохраняет реальную мебель и читаемый текст без повторной генерации изображения.
- Числа и комплектация должны соответствовать конкретному ID. Источник и дата — в журнале, покупательские подписи без служебных комментариев.
- Новый тип проверяется tools/audit-catalog.js. Счётчик обозначает слайды, а не выдаёт текстовый слайд за дополнительное фото.

### Проверка размеров ID 91

05.09.2026 пользователь отозвал ранее сообщённые 366×160×800 и 318×160: они относились к другой модели. Эти значения не применять к ID 91. Новый источник — https://mebvektor.ru/products/divan-lorton-dku-o . Итог проверки и изменения фиксируются в docs/catalog/CHANGELOG.md.

Итоговое уточнение пользователя для ID 91 от 05.09.2026: корпус 258×160 см, спальное место 212×160 см, по каталогу производителя. Высота 80 см сохранена из прежних данных. Значения 366/318 отозваны. Слайд размеров восстанавливать только по реальному кадру разложенной модели.

05.09.2026 пользователь предоставил пример раскладки ID 91 и уточнение: левая часть спинки тоже опускается, формируя ровное место. Подключён slide_dims-v2.png (третий слайд): верх 258×160, низ 212×160 см. Это визуализация на основе главного фото и примера пользователя; не называть реальной фотографией. Горчичный цвет и идентичность предмета сохранять. Предыдущее ожидание примера раскладки закрыто этим уточнением.

## Решения от 25.09.2026: слайды размеров и карточки серий

### Отбор фотографий для слайда «Размеры»

- Плохое, нечёткое или композиционно непригодное фото не использовать только ради заполнения слайда.
- Фото другой версии модели запрещено, даже если отличается лишь подголовник, спинка, подушка или прострочка. Такой материал удалить из галереи либо не подключать.
- Для `Тахта Найс` утверждено исключение: слайд размеров строится по одному проверенному фото товара. Нельзя добавлять второй вид, если нет достоверного фото этой же версии в разложенном состоянии.
- На однопанельном слайде товар и все размерные линии должны находиться в одном кадре. Линии располагаются строго параллельно соответствующим видимым граням товара, с одинаковыми перпендикулярными отступами у обоих концов.
- В карточке и `dims` размеры хранятся в миллиметрах. На самом графическом слайде допустимы сантиметры без единиц в капсулах, если это соответствует принятому стилю проекта.
- Перед публикацией слайд обязательно показывается внутри полной карточки. Публикация — только после явного одобрения пользователя.

### Объединение товаров в серию

- Родственные модели можно объединять в одну полноширинную карточку серии через `PRODUCT_SERIES`. Исходные товары, их `id`, `PRODUCTS`, `IMAGES` и `GALLERIES` не удалять: кнопка варианта должна открывать обычную полную карточку модели.
- `memberIds` задаёт утверждённый порядок вариантов. Рендерить участников именно в этом порядке, а не повторно сортировать по алфавиту.
- Серия участвует в поиске и фильтрах как одна карточка, но счётчик моделей считает все входящие товары. Если по поиску найден только один участник, показывать обычную карточку товара; группировка полезна только при двух и более совпадениях.
- Для матрасов в кнопке варианта показывать короткое имя без названия серии и слова «Матрас», затем высоту и допустимую нагрузку. Неподтверждённые значения не додумывать.
- Для длинных серий по умолчанию показывать четыре модели и кнопку «Показать все N моделей»; после раскрытия показывать все варианты и кнопку «Свернуть».
- Утверждённые серии матрасов: `Нова` (174, 215), `Реал` (254–256), `Орто` (257–259), `Оптима` (260–265), `Элит` (266–267).
- После изменения серии проверить: категорию «Матрасы», поиск по названию серии, открытие каждой модели, раскрытие длинной серии, корректный счётчик и мобильную ширину.
